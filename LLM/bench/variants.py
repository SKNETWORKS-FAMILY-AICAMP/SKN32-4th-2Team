"""프롬프트 변형 모음 (측정용).

`app/prompts.py` 가 운영 프롬프트의 단일 출처다. 여기 있는 것은 **후보**다.
벤치로 재서 이긴 변형만 `app/prompts.py` 로 승격하고, 여기에는 비교 기록으로
남긴다. 그래야 보고서에 "왜 이 프롬프트인가" 를 근거와 함께 쓸 수 있다.

    python bench/run_bench.py --provider openai --variant catdef

왜 파일을 직접 고치지 않는가
---------------------------
조건마다 `prompts.py` 를 고쳐 돌리면 비교가 커밋 사이에 흩어져, 나중에
"그때 baseline 이 정확히 뭐였지" 를 복원할 수 없다. 한 커밋 안에 후보가
전부 있어야 재현된다.

주의: 서비스 모듈들이 `from app.prompts import X` 로 **값을 복사해** 가지므로
`app.prompts` 를 고쳐도 이미 임포트된 쪽은 안 바뀐다. 그래서 소비하는 모듈의
속성을 직접 갈아끼운다.
"""

from __future__ import annotations

from app import prompts
from app.domain import TOPIC_CATEGORIES

# --- 후보 1: catdef — 카테고리에 정의를 붙인다 ---------------------------
#
# baseline 은 카테고리 **이름 8개만** 주고 뜻을 안 알려준다. 그러면 모델이
# 글자 겹침으로 추측한다. 실제로 34문항 중 오답 4건이 나왔는데 3건이 같은
# 원인이었다 — 질문에 "복무" 가 들어가면 무조건 `복무/징계` 로 갔다.
#
#   "휴직 중인 직원의 **복무** 관리"      휴가/휴직  → 복무/징계
#   "직원 **복무** 관련 기본 규정"        근태/근무형태 → 복무/징계
#   "국외 출장 사전 승인"                근태/근무형태 → 복무/징계
#
# `복무/징계` 는 '비위·징계' 를 뜻하려고 지은 이름인데, 한국어에서 "복무" 는
# 그냥 '근무한다' 는 뜻으로도 널리 쓰인다. 이름만으로는 구분이 안 되므로
# 경계를 말로 적어준다.
#
# **주의 — 아래 후보들에 적힌 숫자는 카테고리 이름이 `복무/징계` 이던 시절의
# 측정값이다.** 그 뒤 이름을 `징계/행동강령` 으로 바꿨고(원인이 이름 자체였다.
# `app/prompts.py` 주석 참조), 후보 본문의 항목 이름도 따라 바꿨다. 분류에
# 넘어가는 enum 은 `TOPIC_CATEGORIES` 에서 오므로 옛 이름 그대로는 돌릴 수 없다.
# 지금 이 후보들을 다시 돌리면 "설명 유무" 는 그때와 같지만 "이름" 은 새 것이라,
# 기록된 숫자와 정확히 같게 나오지는 않는다.
_CATEGORY_DEFINITIONS = """- 휴가/휴직: 연차·병가·경조사·출산휴가·육아휴직 등 **자리를 비우는 것**.
  휴직자의 처우와 복무 관리도 여기입니다.
- 근태/근무형태: 근무시간·유연근무·시간선택제·출장·재택 등 **어떻게 일하는가**.
  복무규정 일반 문의(근무시간, 휴가 절차 등)도 여기입니다.
- 급여/보수: 임금·각종 수당·퇴직금·연봉 등 **받는 돈**.
- 채용/임용: 사람을 뽑고 임용하는 절차. 계약직·별정직·공무직·교원 등
  **특정 고용형태의 임용·인사관리 지침** 문의도 여기입니다.
- 인사/승진: 승진·전보·평가 등 **재직 중의 인사 이동**.
- 복리후생: 의료비 지원, 복지 제도 등 급여 외 혜택.
- 징계/행동강령: 행동강령 위반, 음주운전, 직무범죄, 품위유지 의무 등
  **잘못에 대한 책임**을 묻는 문의. 근무형태나 휴가 문의는 질문에 "복무"라는
  단어가 들어가더라도 여기가 아닙니다.
- 기타: 위 어디에도 맞지 않거나 인사 업무와 무관한 문의."""

TOPIC_SYSTEM_CATDEF = f"""당신은 사내 HR 문의를 분류하는 시스템입니다.
사용자의 질문이 아래 카테고리 중 어디에 속하는지 **정확히 하나만** 고르세요.

카테고리:
{_CATEGORY_DEFINITIONS}

분류 기준:
- 문서 이름이 아니라 **사용자가 무엇을 알고 싶어하는지(질문 의도)** 를 기준으로 판단하세요.
- 예를 들어 근로기준법은 임금·근로시간·연차·해고를 모두 다루므로, 근거 문서가 근로기준법이라는
  사실만으로 카테고리를 정하면 안 됩니다.
- 질문에 카테고리 이름과 같은 단어가 들어 있어도, **뜻이 다르면** 그 카테고리가 아닙니다.
- 두 카테고리에 걸치면 질문의 핵심에 더 가까운 쪽을 고르세요.
- 인사 업무와 무관하거나 어느 쪽에도 맞지 않으면 "기타"를 고르세요."""


# --- 후보 2: fewshot — catdef 에 경계 사례 예시를 붙인다 -------------------
#
# 정의만으로 부족하면 실제 사례를 보여주는 편이 강하다. 다만 예시는 벤치
# 문항과 겹치면 안 된다(그건 정답을 알려주는 것이라 측정이 무의미해진다).
# 아래 6건은 questions.yaml 에 없는 문장으로 새로 지었다.
_TOPIC_EXAMPLES = """
분류 예시:
- "병가 쓰려면 진단서가 필요한가요?" → 휴가/휴직
- "복무 시간이 몇 시부터 몇 시까지인가요?" → 근태/근무형태 ("복무"가 들어갔지만 근무시간 문의)
- "출장 중 숙박비는 얼마까지 나오나요?" → 급여/보수
- "인턴을 정규직으로 전환할 수 있나요?" → 채용/임용
- "근무 중 사적 이익을 취하면 어떻게 되나요?" → 징계/행동강령
- "주차장은 어디에 있나요?" → 기타"""

TOPIC_SYSTEM_FEWSHOT = TOPIC_SYSTEM_CATDEF + "\n" + _TOPIC_EXAMPLES


# --- 후보 3: catdef2 — catdef 이 만든 새 충돌을 고친다 ---------------------
#
# catdef 은 오답 4건 중 3건을 잡았지만 없던 오답을 하나 만들었다.
#
#   "직원 **인사 규정**의 기본 내용"     인사/승진 → 채용/임용
#
# 원인은 내가 채용/임용 정의에 "**인사관리** 지침" 이라고 쓴 것이다. 정의를
# 쓰면서 다른 카테고리 이름과 겹치는 단어를 넣었으니, 이름만 주던 baseline 과
# 똑같은 실수를 정의 안에서 반복한 셈이다.
#
# 그래서 두 카테고리를 **겹치지 않는 말로** 다시 가른다.
#   채용/임용  = 조직에 **들어오는** 시점 (특정 고용형태로 임용되는 것 포함)
#   인사/승진  = 이미 들어온 뒤의 **이동과 제도 전반**
_CATEGORY_DEFINITIONS_V2 = """- 휴가/휴직: 연차·병가·경조사·출산휴가·육아휴직 등 **자리를 비우는 것**.
  휴직자의 처우와 복무 관리도 여기입니다.
- 근태/근무형태: 근무시간·유연근무·시간선택제·출장·재택 등 **어떻게 일하는가**.
  '복무규정'이 무엇인지 묻는 일반 문의도 여기입니다(복무규정은 근무시간과
  휴가 절차를 정한 문서이지, 징계 문서가 아닙니다).
- 급여/보수: 임금·각종 수당·퇴직금·연봉 등 **받는 돈**.
- 채용/임용: 조직에 **들어오는** 시점의 문의. 채용 절차, 그리고 계약직·별정직·
  공무직·교원 등 **특정 고용형태로 임용**되는 사람에 관한 규정.
- 인사/승진: 이미 재직 중인 직원의 **이동과 인사 제도 전반**. 승진·전보·평가,
  그리고 직원 인사 규정 일반 문의.
- 복리후생: 의료비 지원, 복지 제도 등 급여 외 혜택.
- 징계/행동강령: 행동강령 위반, 음주운전, 직무범죄, 품위유지 의무 등
  **잘못에 대한 책임**을 묻는 문의. 근무형태나 휴가 문의는 질문에 "복무"라는
  단어가 들어가더라도 여기가 아닙니다.
- 기타: 위 어디에도 맞지 않거나 인사 업무와 무관한 문의."""

TOPIC_SYSTEM_CATDEF2 = TOPIC_SYSTEM_CATDEF.replace(
    _CATEGORY_DEFINITIONS, _CATEGORY_DEFINITIONS_V2
)


# --- 후보 4: korean — 언어를 프롬프트 맨 앞에서 못박는다 -------------------
#
# Qwen2.5:7b 가 한국어 질문에 **중국어로 답한다.** 34문항 중 4~5건이 새는데,
# 한 건은 한자 1601자 vs 한글 132자로 사실상 전문이 중국어였다.
# (OpenAI 는 같은 프롬프트로 0/34. 모델 고유의 성향이다)
#
# 운영 프롬프트에도 "한국어 존댓말로" 라는 지시가 답변 규칙 4번에 이미 있다.
# 무시당한 이유는 그게 **규칙 목록 중간에 묻혀 있어서** 로 보인다. 그래서
# 맨 앞 단독 문단으로 올리고, 위반 시 무엇을 하라는지까지 적는다.
# 5건 → 3건. **없애지는 못했다.** 그래서 Qwen 은 답변 생성용으로 못 쓴다는
# 결론은 그대로다. 다만 비용이 사실상 없고 방향이 맞아 승격했다.
#
# 승격됐으므로 이 후보는 `app/prompts.py` 와 같은 값이다. 아래 문자열은
# 승격 전 문안을 되살리기 위한 **역방향** 정의다 — 지금의 운영 프롬프트에서
# 지시 문단을 떼어내면 실험 당시의 "지시 없음" 상태가 된다.
_KOREAN_DIRECTIVE = """**출력 언어: 한국어.**
질문이 어떤 언어이든, 참고 문서가 어떤 언어이든 답변은 반드시 한국어로 작성합니다.
중국어·영어·일본어 문장을 쓰지 마세요. 한자는 법령명 병기 등 꼭 필요한 경우에만
괄호 안에 씁니다.

"""

if not prompts.ANSWER_SYSTEM.startswith(_KOREAN_DIRECTIVE):
    raise SystemExit(
        "prompts.ANSWER_SYSTEM 앞의 언어 지시 문단이 바뀌었습니다. "
        "variants.py 의 _KOREAN_DIRECTIVE 를 맞춰 주세요."
    )

# 지시 문단을 뗀 상태 = 이 실험의 '개선 전'
ANSWER_SYSTEM_NO_LANG = prompts.ANSWER_SYSTEM[len(_KOREAN_DIRECTIVE) :]


# --- 후보 5: cite-negative — 조문 인용 규칙을 금지형으로 쓴다 --------------
#
# 운영 프롬프트(3번 규칙)는 **긍정형 기본값**을 준다.
#   "출처는 규정 이름까지만 쓰는 것이 기본. 조 번호가 눈에 보일 때만 더하라"
#
# 처음에는 금지형으로 썼었다.
#   "제N조가 실제로 적혀 있을 때만 인용하라. 항·호 번호만 있는 것은 인용이
#    아니다. '제2항에 명시되어 있습니다' 라고 쓰면 안 된다"
#
# 손으로 몇 개 던져 보니 금지형은 "근로기준법 제2항" 같은 형식 오류를 냈고,
# 긍정형은 "근로기준법 제57조" 처럼 **그럴듯한데 틀린** 조문을 대기도 했다.
# 후자가 더 위험해 보였지만 표본이 각 1건이라 판단할 수 없었다.
# 그래서 `bad_citations` 지표를 만들어 35문항으로 비교한다.
_CITE_NEGATIVE = """3. 조문 번호는 **[참고 문서]에 `제N조` 형태로 실제로 적혀 있을 때만** 인용하세요.
   기억으로 번호를 붙이면 안 됩니다. 문서가 조문 중간부터 시작해 `제N조`가
   안 보이면 **번호 없이 내용으로만** 답하세요.
   `제2항`·`제3호`처럼 항·호 번호만 있는 것은 인용이 아닙니다. 조 번호 없이
   "제2항에 명시되어 있습니다" 라고 쓰면 안 됩니다.
   수치와 표현도 문서에 적힌 대로 쓰세요. (문서가 "통상임금의 100분의 50을
   가산"이라고 하면 그렇게 쓰고, "1.5배" 처럼 바꿔 말하지 마세요)"""

_CITE_POSITIVE_START = "3. 출처를 밝힐 때는"
_CITE_POSITIVE_END = '"1.5배" 처럼 바꿔 말하지 마세요)'


def _swap_rule3(text: str, replacement: str) -> str:
    start = text.index(_CITE_POSITIVE_START)
    end = text.index(_CITE_POSITIVE_END, start) + len(_CITE_POSITIVE_END)
    return text[:start] + replacement + text[end:]


ANSWER_SYSTEM_CITE_NEGATIVE = _swap_rule3(prompts.ANSWER_SYSTEM, _CITE_NEGATIVE)


# --- v0: 승격 이전의 운영 프롬프트 (박제) ---------------------------------
#
# catdef2 를 `app/prompts.py` 로 승격했으므로, 이제 `--variant baseline` 은
# 개선된 프롬프트를 돌린다. 그러면 보고서의 "개선 전" 수치를 다시 뽑을 방법이
# 없어진다 — git 을 뒤져 예전 파일을 꺼내와야 한다.
#
# 성능 보고서의 핵심이 전후 비교이므로, 개선 전 문안을 여기 그대로 남긴다.
# 이 문자열은 **고치지 말 것.** 고치는 순간 기록된 88% 를 재현할 수 없다.
TOPIC_SYSTEM_V0 = f"""당신은 사내 HR 문의를 분류하는 시스템입니다.
사용자의 질문이 아래 카테고리 중 어디에 속하는지 **정확히 하나만** 고르세요.

카테고리:
{chr(10).join(f"- {c}" for c in TOPIC_CATEGORIES)}

분류 기준:
- 문서 이름이 아니라 **사용자가 무엇을 알고 싶어하는지(질문 의도)** 를 기준으로 판단하세요.
- 예를 들어 근로기준법은 임금·근로시간·연차·해고를 모두 다루므로, 근거 문서가 근로기준법이라는
  사실만으로 카테고리를 정하면 안 됩니다.
- 두 카테고리에 걸치면 질문의 핵심에 더 가까운 쪽을 고르세요.
- 인사 업무와 무관하거나 어느 쪽에도 맞지 않으면 "기타"를 고르세요."""


# --- 등록 -----------------------------------------------------------------
# 값이 비면 운영 프롬프트(`app/prompts.py`)를 그대로 쓴다.
VARIANTS: dict[str, dict[str, str]] = {
    "baseline": {},
    "v0": {"TOPIC_SYSTEM": TOPIC_SYSTEM_V0},
    "catdef": {"TOPIC_SYSTEM": TOPIC_SYSTEM_CATDEF},
    "fewshot": {"TOPIC_SYSTEM": TOPIC_SYSTEM_FEWSHOT},
    "catdef2": {"TOPIC_SYSTEM": TOPIC_SYSTEM_CATDEF2},
    "nolang": {"ANSWER_SYSTEM": ANSWER_SYSTEM_NO_LANG},
    "cite-negative": {"ANSWER_SYSTEM": ANSWER_SYSTEM_CITE_NEGATIVE},
}

# 상수 이름 → 그 값을 실제로 쓰는 모듈 경로.
# `from ... import X` 는 값을 복사하므로 원본이 아니라 소비처를 갈아끼워야 한다.
_CONSUMERS = {
    "TOPIC_SYSTEM": "app.services.topic",
    "ANSWER_SYSTEM": "app.services.answer",
    "CHATROOM_NAME_SYSTEM": "app.services.naming",
}


def apply(name: str) -> dict[str, str]:
    """변형을 적용하고, 실제로 바뀐 항목을 돌려준다."""
    if name not in VARIANTS:
        raise SystemExit(f"모르는 변형 '{name}'. 가능한 값: {', '.join(VARIANTS)}")

    import importlib

    overrides = VARIANTS[name]
    for const, module_path in _CONSUMERS.items():
        if const not in overrides:
            continue
        module = importlib.import_module(module_path)
        if not hasattr(module, const):
            raise SystemExit(
                f"{module_path} 에 {const} 이 없습니다. "
                "임포트 방식이 바뀌었는지 확인하세요."
            )
        setattr(module, const, overrides[const])
    return overrides


def check_categories() -> None:
    """각 후보가 카테고리 8종을 빠짐없이 설명하는지 확인한다.

    `domain.py` 에서 카테고리를 늘렸는데 후보 문안을 안 늘리면, 모델은 그
    카테고리를 영영 못 고르면서도 오류는 안 난다. 조용히 틀리는 쪽이 위험하다.
    (운영 프롬프트에는 `app/prompts.py` 가 같은 검사를 따로 갖고 있다.
     v0 은 카테고리를 이름만 나열하므로 이 검사 대상이 아니다.)
    """
    for name, text in (("catdef", _CATEGORY_DEFINITIONS), ("catdef2", _CATEGORY_DEFINITIONS_V2)):
        missing = [c for c in TOPIC_CATEGORIES if f"- {c}:" not in text]
        if missing:
            raise SystemExit(f"variants.py 의 {name} 에 설명이 없는 카테고리: {missing}")
