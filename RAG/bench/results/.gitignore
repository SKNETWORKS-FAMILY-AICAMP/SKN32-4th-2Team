# Per-run benchmark outputs stay local. Only compact summaries and the corpus
# manifest are versioned so reports remain reproducible without raw-result bloat.
*.json
*.jsonl
*.csv

!*-summary.json
!*-summary.csv
!*-manifest.json
