# Copy this file to .env and fill only local or deployment-specific values.
# Never commit the real .env file.

# API
RAG_API_HOST=0.0.0.0
RAG_API_PORT=8001
RAG_CORS_ORIGINS=http://127.0.0.1:8000,http://localhost:8000

# MySQL
RAG_DB_HOST=127.0.0.1
RAG_DB_PORT=3306
RAG_DB_USER=change-me
RAG_DB_PASSWORD=change-me
RAG_DB_NAME=rag_chatbot

# Retrieval runtime
# CPU에서 안전하게 시작하고, CUDA 지원 PyTorch와 GPU가 있으면 자동으로 가속합니다.
# 성능 비교나 문제 진단 때만 cpu 또는 cuda로 강제하세요.
RAG_DEVICE=auto
# Set to 0 only when comparing the old chunking behavior. Reindex after changing it.
# RAG_ARTICLE_HEAD=1
# RAG_CHUNK_SIZE=400
# RAG_CHUNK_OVERLAP=80
# RAG_MIN_CHUNK_LENGTH=80
# RAG_EMBEDDING_MODEL=jhgan/ko-sroberta-multitask
# RAG_RERANKER_MODEL=BAAI/bge-reranker-v2-m3
# 서버가 준비 완료를 출력하기 전에 문서별 FAISS 캐시를 미리 읽습니다.
# 메모리가 매우 부족한 개발 PC에서만 0으로 바꾸면 첫 검색이 느려집니다.
RAG_WARM_VECTOR_STORES=1
RAG_SEARCH_TOP_K=5
RAG_SEARCH_INITIAL_CANDIDATES=20
